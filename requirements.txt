# Core dependencies - pinned for reproducibility
# GitHub AI Trend Tracker - Minimal Requirements

# Data ingestion and loading
dlt[duckdb,parquet]==1.22.0  # Added parquet support for pyarrow
duckdb==1.4.4
pyarrow==15.0.0
requests==2.31.0

# Data transformations
dbt-core==1.10.0
dbt-duckdb==1.9.0

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2

# Development (optional - remove for production if needed)
pytest==8.0.0
